<% include ../template/header %>

<div class="">
  

  <div class="clearfix"></div>
  
   <% if(messages.info)

    {messages.info.forEach(function(info){%>
        <div class="alert alert-success"><%= info %></div>
   <% })} %>

   <% if(messages.error)

      {messages.error.forEach(function(erro){%>
          <div class="alert alert-danger"><%= erro %></div>
    <% })} %>

  <div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
      <div class="x_panel">
        <div class="x_title">
          <h2>Lista de contatos</h2>
          <ul class="nav navbar-right panel_toolbox">
          </ul>
          <div class="clearfix"></div>
        </div>
        <div class="x_content">
         
          <table id="datatable" class="table table-striped table-bordered">
            <thead>
              <tr>
                <th>Nome</th>
                <th>Celular</th>
                <th>Email</th>
                <th>Tipo de Contato</th>
                <th> Ações </th>
              </tr>
            </thead>

            <tbody>
                <% list.forEach(function(item) { %>
                  <tr>
                    <td><%= item.nome %></td>
                    <td><%= item.celular1 %></td>
                    <td><%= item.emal %></td>
                    <td><%= item.tipo_de_contato %></td>
                    <td> 
                      <a class="btn btn-info" target="_blank" href="/contato/show?id=<%= item.id %>" /a> Visualizar</a> | <a class="btn btn-primary" href="/contato/edit?id=<%= item.id %>" a> Editar</a> 
                      <%if (role == 'admin') {%>  | <a class="btn btn-danger " href="/contato/desabilitar?id=<%= item.id %>"  onclick="return confirm('Deseja cancelar?');"> Excluir </a> <%}%> </td>  
                  </tr>
                <% }); %>
            </tbody>
          </table> 
        </div>
      </div>
    </div>
    </div>
  </div>
</div>


<%if (3==4) {%>  TEXT NOT PRINTED  <%}%>

<% include ../template/footer %>